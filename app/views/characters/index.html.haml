%h1 Characters

%p= link_to("New Character", new_character_url)

%table.table
  %tr
    %th Name
  - @characters.each do |character|
    %tr
      %td= link_to(character.name, edit_character_url(character))

